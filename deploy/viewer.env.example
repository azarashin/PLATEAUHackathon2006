# Copy this file outside the repository and set the values on the server.
# For the allowed host value, enter hostnames only without a protocol, path, or port.
__VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS=
# Keep bind addresses and port numbers specific to the deployment environment.
VIEWER_BIND_HOST=
VIEWER_PORT=
VIEWER_HTTP_PORT=
